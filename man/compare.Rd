% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-class.R
\name{compare}
\alias{compare}
\title{Compare True Parameters to Estimated Quantiles}
\usage{
compare(
  object,
  start = 1L,
  end = NULL,
  ps = NULL,
  probability = c(0.05, 0.5, 0.975),
  verbose = TRUE
)
}
\arguments{
\item{object}{A fitted instance from the a posterior class}

\item{start}{First iteration to include in comparison (default: 1)}

\item{end}{Last iteration to include in comparison (default: NULL uses all samples)}

\item{ps}{Named vector of true parameter values to compare against estimates}

\item{probability}{Vector of quantiles to compute (default: c(0.05, 0.5, 0.975))}

\item{verbose}{Logical Whether to print results (default: TRUE)}
}
\value{
A matrix with rows containing:
\itemize{
  \item True parameter values
  \item Estimated quantiles (labeled with percentage)
  \item Bias (Median - True)
}
}
\description{
Compares true parameters to posterior quantiles from a fitted model object 
and calculates estimation accuracy.
}
\details{
This function:
\itemize{
  \item Extracts quantile summaries from the posterior samples
  \item Validates parameter names match between true values and estimates
  \item Computes estimation bias using the median
  \item Returns (and optionally prints) a comparison matrix
}
}
\examples{
\dontrun{
model <- ggdmcModel::BuildModel(
    p_map = list(A = "1", B = "1", mean_v = "M", sd_v = "1", st0 = "1", t0 = "1"),
    match_map = list(M = list(s1 = "r1", s2 = "r2")),
    factors = list(S = c("s1", "s2")),
    constants = c(sd_v = 1, st0 = 0),
    accumulators = c("r1", "r2"),
    type = "lba"
)

fits0 <- ggdmc::StartSampling_subject(sub_dmis[[1]], sub_priors,
    sub_migration_prob = 0.00, thin = 1L, seed = 9032
)
fit0 <- ggdmc::RebuildPosterior(fits0)
options(digits = 2)
est_phi <- compare(fit0, ps = p_vector)
}

}
\seealso{
\code{\link{compare_many}} for the function compare multiple posterior objects
\code{\link{summary}} for the function summarises a posterior object used in the 
situation of fitting empirical data
\code{\link{summary_many}} for the function summarises multiple posterior objects
}
