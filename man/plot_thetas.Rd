% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model-class.R
\name{plot_thetas}
\alias{plot_thetas}
\title{Plot Theta Parameters using Lattice Graphics}
\usage{
plot_thetas(
  x,
  start = 1L,
  end = NULL,
  subchain = FALSE,
  chains = NA,
  hide_legend = TRUE,
  subjects = NULL,
  seed = NULL,
  max_subjects = 50
)
}
\arguments{
\item{x}{A data frame/table containing columns: Iteration, value, Chain, and 
s (subject label)}

\item{start}{First iteration to include (default: 1)}

\item{end}{Last iteration to include (default: NULL uses all samples)}

\item{subchain}{Logical Whether to plot a subset of chains (default: FALSE)}

\item{chains}{Numeric vector specifying which chains to include when subchain=TRUE
(default: NA includes all chains)}

\item{hide_legend}{Logical Whether to hide legend (default: TRUE)}

\item{subjects}{Number of subjects to plot or vector of specific subjects:
- NULL (default): plots all subjects, if less than 5. Plot the first 5, when
                  the subject number exceeeds 5.
- Integer N: randomly samples N subjects
- Character vector: plots specified subjects}

\item{seed}{(Optional) random seed for reproducible sampling (default: NULL)}

\item{max_subjects}{(Optional) The maximum number of subjects to plot. If the
posterior object contains many subjects, setting this argument to at most 
\code{max_subjects} (default: 50). Useful for avoiding overplotting with large 
datasets.}
}
\value{
Invisibly returns the lattice plot object. Primarily called for side effects.
}
\description{
Visualises theta parameter traces using lattice graphics with faceting by parameter.
Provides flexible subject selection options.
}
\examples{
\dontrun{
# Prepare data
fits <- fits1
phi <- RebuildHyper(fits)
thetas <- RebuildPosteriors(fits)

DT <- ggdmc::prepare_thetas_data(fits[[1]]$subject_theta, start = 250)
DT <- ggdmc::prepare_thetas_data(thetas, start = 5000)
p1 <- plot_thetas(DT)
p1 <- plot_thetas(DT, start = 300, end = 400)
p1 <- plot_thetas(DT, start = 300, end = 400, subjects = 5)
p1 <- plot_thetas(DT, start = 300, end = 400, subjects = as.character(1:10))
p1 <- plot_thetas(DT, start = 300, end = 400, max_subjects = 8)
}

}
